#LAB 1 - Carlinfg
#Destiné uniquement à initialiser un environnement vagrant et à provisionner une première VM

#initialisation de l'environnement
vagrant init ubuntu/trusty64

#Démarrage de la machine 
vagrant up

#Connexion en ssh 
vagrant ssh

#Arrêt et suppression de la machine 
vagrant halt
vagrant destroy
